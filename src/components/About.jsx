import { Helmet } from "react-helmet-async";

const About = () => {
    return (
        <div>
             <Helmet>
        <title>About || context api</title>
    </Helmet>
            <h1>this is about page</h1>
        </div>
    );
};

export default About;